"use client";

import { useEffect } from "react";

// This hook is a placeholder for syncing a Google Doc preview in real-time.
// In a real application, it could poll an API or subscribe to doc change events.
export function useGoogleDocSync(docUrl: string) {
  useEffect(() => {
    // No operation performed here yet
  }, [docUrl]);
}